# 1. 基础设置
work_dir: ./work_dir/SHREC/jmotion_finetune # ⚠️ 修改目录
stream: jmotion

# 2. 关键：加载最佳权重
weights: ./work_dir/SHREC/jmotion/epoch015_acc88.69_model.pt

# 3. 训练参数调整
phase: train
start_epoch: 15
num_epoch: 60
base_lr: 0.005         # 🔴 降低 LR，稳定参数
step: [20, 40]         # 稍早一点衰减，因为 Jmotion 很容易崩
warm_up_epoch: 0

# 4. 正则化增强
lambda_entropy: 0.05   # 🔴 增大熵惩罚，抑制噪声
lambda_ortho: 0.1

# 5. 模型定义
model: net.dsa_hgn.Model
model_args:
  in_channels: 3
  base_channels: 64
  ch_ratio: 2
  num_stages: 10
  inflate_stages: [5, 8]
  down_stages: [5, 8]
  data_bn_type: 'VC'
  num_person: 1
  pretrained: None
  num_class: 14
  num_point: 22
  drop_out: 0.2        # 如果还过拟合，可以考虑改为 0.2
  graph: graph.shrec.Graph
  graph_args:
    labeling_mode: 'spatial'
  num_hyperedges: 16
  k_neighbors: 4
  adaptive: true
  use_virtual_conn: True

# 6. 数据加载 (注意 vel: True)
train_feeder: feeder.feeder_egogesture.Feeder
test_feeder: feeder.feeder_egogesture.Feeder
train_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_label.pkl
  split: train
  bone: False
  vel: True            # 🔴 保持开启
  window_size: 180
  normalization: False
  random_choose: True
  random_move: False
  random_shift: True
  repeat: 5
  debug: False
  use_mmap: True
test_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_label.pkl
  split: test
  bone: False
  vel: True            # 🔴 保持开启
  window_size: 180
  normalization: False
  debug: False
  use_mmap: True

# 7. 其他
optimizer: SGD
weight_decay: 0.0005
nesterov: True
grad_clip_norm: 1.0
lr_decay_rate: 0.1
device: [0]
batch_size: 32
test_batch_size: 32
num_worker: 4
loss: torch.nn.CrossEntropyLoss
log_interval: 100
save_interval: 1
eval_interval: 1