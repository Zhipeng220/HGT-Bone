work_dir: ./work_dir/SHREC/bone1

stream: bone

model: net.dsa_hgn.Model
model_args:
  in_channels: 8
  base_channels: 64
  num_class: 14
  num_person: 1

  graph: graph.shrec_line.Graph
  graph_args:
    labeling_mode: 'spatial'
  num_point: 21

  # ✅ 修复 1: 增加动态超边数量,补偿物理边带来的表达能力损失
  num_hyperedges: 12       # 12 动态 + 5 物理 = 17 总边 (超过原始 16)
  k_neighbors: 4
  adaptive: true
  use_virtual_conn: True

  num_stages: 10
  inflate_stages: [5, 8]
  down_stages: [5, 8]
  data_bn_type: 'VC'
  drop_out: 0.3
  temperature: 0.05

train_feeder: feeder.feeder_hgt.Feeder
test_feeder: feeder.feeder_hgt.Feeder

train_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_label.pkl
  split: train
  random_choose: True
  random_rot: True
  repeat: 5
  normalization: False
  window_size: 180
  debug: False
  use_mmap: True
  use_sg: True
  sg_window: 9
  sg_order: 2
  use_angles: True
  use_rotations: True

test_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_label.pkl
  split: test
  debug: False
  use_mmap: True
  normalization: False
  window_size: 180

  use_sg: True
  sg_window: 9
  sg_order: 2
  use_angles: True
  use_rotations: True

# ===================================================================
# 🔧 修复方案 3: 平衡正则化权重
# ===================================================================

optimizer: SGD
base_lr: 0.05
weight_decay: 0.0005
nesterov: True
grad_clip_norm: 1.0

lr_decay_rate: 0.1
step: [30, 50]
warm_up_epoch: 5
num_epoch: 80

device: [0]
batch_size: 32
test_batch_size: 32
num_worker: 4

loss: torch.nn.CrossEntropyLoss

lambda_entropy: 0.05    # 增加熵权重,压制均匀分布 (0.001 → 0.005)
lambda_ortho: 0.05       # 减小正交约束,增加灵活性 (0.1 → 0.05)
lambda_physical: 0.05    # ⭐ 启用物理约束! (0.0 → 0.05)

log_interval: 100
save_interval: 1
eval_interval: 1
